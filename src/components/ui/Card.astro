---
const { as = "article", tone = "default", class: className, ...rest } = Astro.props;
const Tag = as;
const classes = ["ui-card", "card", tone !== "default" ? `ui-card--${tone}` : null, className];
---

<Tag class:list={classes} {...rest}>
    <slot />
</Tag>

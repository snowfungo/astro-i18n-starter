---
const { tone = "default", title, class: className, ...rest } = Astro.props;
---

<div class:list={["ui-alert", tone !== "default" ? `ui-alert--${tone}` : null, className]} {...rest}>
    {title && <strong>{title}</strong>}
    <slot />
</div>
